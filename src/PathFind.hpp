#ifndef JE_PATHFIND_HPP
#define JE_PATHFIND_HPP

namespace je
{

template <typename G>
void findSinglePath(std::vector<sf::Vector2f>& results, G& graph, const sf::Vector2f& source, std::initializer_list<sf::Vector2f> destinations)
{

}

template <typename G>
void findSingleUnweightedPath(std::vector<sf::Vector2f>& results, G& graph, const sf::Vector2f& source, std::initializer_list<sf::Vector2f> destinations)
{

}

}

#endif
